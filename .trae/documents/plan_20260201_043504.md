# 修复Redis序列化Java 8日期时间类型的问题

## 问题分析

用户遇到了Redis序列化异常，错误信息显示：

```
org.springframework.data.redis.serializer.SerializationException: Could not write JSON: Java 8 date/time type `java.time.LocalDateTime` not supported by default: add Module "com.fasterxml.jackson.datatype:jackson-datatype-jsr310" to enable handling (through reference chain: com.mojian.entity.SysConfig["updateTime"])
```

**原因**：
1. 系统中使用了Java 8的LocalDateTime类型（在SysConfig实体的updateTime字段）
2. Jackson默认不支持Java 8的日期时间类型
3. 需要为Jackson2JsonRedisSerializer配置JavaTimeModule

## 修复方案

### 修改RedisConfig配置类，为Jackson2JsonRedisSerializer添加JavaTimeModule配置

**修改文件**：`blog/mojian-commom/src/main/java/com/mojian/config/RedisConfig.java`

**修改内容**：

1. **添加必要的导入**：
   ```java
   import com.fasterxml.jackson.databind.ObjectMapper;
   import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
   import com.fasterxml.jackson.databind.SerializationFeature;
   ```

2. **修改redisTemplate方法**：
   ```java
   @Bean
   public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
       RedisTemplate<String, Object> template = new RedisTemplate<String, Object>();
       template.setConnectionFactory(factory);

       // 配置ObjectMapper，支持Java 8日期时间类型
       ObjectMapper objectMapper = new ObjectMapper();
       objectMapper.registerModule(new JavaTimeModule());
       objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

       Jackson2JsonRedisSerializer serializer = new Jackson2JsonRedisSerializer(Object.class);
       serializer.setObjectMapper(objectMapper);

       // 使用StringRedisSerializer来序列化和反序列化redis的key值
       template.setKeySerializer(new StringRedisSerializer());
       template.setValueSerializer(serializer);

       // Hash的key也采用StringRedisSerializer的序列化方式
       template.setHashKeySerializer(new StringRedisSerializer());
       template.setHashValueSerializer(serializer);

       template.afterPropertiesSet();
       return template;
   }
   ```

3. **修改cacheManager方法**：
   ```java
   @Bean
   public CacheManager cacheManager(RedisConnectionFactory factory) {
       // 配置ObjectMapper，支持Java 8日期时间类型
       ObjectMapper objectMapper = new ObjectMapper();
       objectMapper.registerModule(new JavaTimeModule());
       objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

       Jackson2JsonRedisSerializer<Object> serializer = new Jackson2JsonRedisSerializer<>(Object.class);
       serializer.setObjectMapper(objectMapper);

       // 为不同缓存设置不同过期时间
       Map<String, RedisCacheConfiguration> configMap = new HashMap<>();
       
       // 为sys_dict缓存设置60分钟过期时间
       configMap.put("sys_dict", RedisCacheConfiguration.defaultCacheConfig()
               .entryTtl(Duration.ofMinutes(60))
               .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()))
               .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(serializer)));
       
       // 默认缓存配置（无过期时间）
       RedisCacheConfiguration defaultConfig = RedisCacheConfiguration.defaultCacheConfig()
               .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()))
               .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(serializer));
       
       // 构建缓存管理器
       return RedisCacheManager.builder(factory)
               .cacheDefaults(defaultConfig)
               .withInitialCacheConfigurations(configMap)
               .build();
   }
   ```

## 修复原理

1. **添加JavaTimeModule**：注册Jackson的JavaTimeModule，使其能够处理Java 8的日期时间类型
2. **禁用时间戳序列化**：设置`disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)`，使日期时间以ISO-8601格式序列化，提高可读性
3. **统一配置**：确保RedisTemplate和CacheManager都使用相同配置的Jackson序列化器

## 预期结果

1. **序列化成功**：Java 8的LocalDateTime类型能够正确序列化到Redis
2. **反序列化成功**：Redis中的日期时间数据能够正确反序列化为LocalDateTime类型
3. **格式友好**：日期时间以ISO-8601格式存储，提高可读性
4. **系统稳定**：不再出现序列化异常，系统能够正常运行

## 验证步骤

1. **重启后端服务**
2. **触发缓存操作**：访问需要缓存的功能，如资源分类查询
3. **检查Redis**：查看Redis中存储的数据，确认日期时间字段格式正确
4. **验证功能**：确保系统功能正常，无序列化异常

此方案能够彻底解决Java 8日期时间类型的Redis序列化问题，确保系统的稳定运行。